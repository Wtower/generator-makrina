          <!-- Field: <%= labelName %> -->
          <!-- Generated by yeoman generator-makrina <%= version %> on <%= date %> -->
          <div class="form-group">
            <label class="control-label col-md-3 col-sm-3 col-xs-12">
              <%= labelName %><% if (required) { %> <span class="required">*</span><% } %>
            </label>
            <div class="col-md-9 col-sm-9 col-xs-12">
              <div class="checkbox">
                <label>
                  <input type="checkbox"
                         id="<%= objectUrl %>-<%= fieldNameKebab %>"
                         name="<%= fullName %>"
                         <% if (readOnly) { %>disabled="disabled"
                         <% } %>ng-model="$ctrl.<%= objectName %>.<%= fieldName %>">
                  <%= labelName %>
                </label>
              </div>
              <!-- here additional checkboxes for the control label -->
            </div>
          </div>
